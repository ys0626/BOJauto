#include <bits/stdc++.h>
#define fast_io cin.tie(NULL); ios_base::sync_with_stdio(false);
using namespace std;
 
int main()
{
    fast_io
    string str;
    int n;

    getline(cin, str);
    cout << str.size();
    
    return 0;
}